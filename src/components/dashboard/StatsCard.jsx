/*import React from 'react';

const StatsCard = ({ title, value, icon, delay }) => {`
  return (
    <div
      className="bg-[#112D4E] p-6 rounded-2xl shadow-xl flex items-center justify-between 
      hover:scale-105 transition duration-300 
      animate-[fadeUp_0.6s_ease]"
      style={{ animationDelay: ${delay}ms }} // تأخير الأنيميشن
    >

      <div>
        <p className="text-slate-400 text-sm">
          {title}
        </p>

        <h2 className="text-3xl font-bold text-white mt-2">
          {value}
        </h2>
      </div>

      <div className="bg-white/10 p-3 rounded-full text-orange-400">
        {icon}
      </div>

    </div>
  );
`};

export default StatsCard;*/